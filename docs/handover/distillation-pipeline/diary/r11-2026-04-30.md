# R11 — Distillation pipeline plumbing · engineer diary · 2026-04-30

Plan: `docs/research/distillation-pipeline/plan.md`
Task: T1 — P1 (identity rename) + P2 (strict-filter retrofit) + P3 (Path B conformance)
Branch: `gemma-harness-lora`, head `454c68cd` (R11 plan commit) on top of `e15fb241` (R10 baseline postscript)

This entry covers the first half of R11 (P1-P3). P4+P5+P6+P7 follow under T3 after intermediate reviewer APPROVE.

## P1 — Identity reconciliation (`gemma4-perfagent` → `browsing-gemma`)

Per `project_lora_name.md`: the canonical adapter name is `browsing-gemma`. Wave 5 (committed 14 days ago) shipped with `gemma4-perfagent` as the adapter directory and `perfagent-smoke-finetune` as the smoke model name. P1 reconciles.

**Touched** (all single-line edits):

- `packages/evals/scripts/distill/build-modelfile.ts:16` — `DEFAULT_ADAPTER` `./adapters/gemma4-perfagent.gguf` → `./adapters/browsing-gemma.gguf`
- `packages/evals/scripts/distill/smoke-finetune.ts:42` — `DEFAULT_SMOKE_MODEL_NAME` `perfagent-smoke-finetune` → `browsing-gemma-smoke`
- `packages/evals/scripts/distill/smoke-finetune.ts:164` — temp dir prefix `perfagent-smoke-` → `browsing-gemma-smoke-`
- `packages/evals/tests/modelfile-builder.test.ts:15,23` — adapter path string in test fixture
- `packages/evals/data/distill/examples/Modelfile` — regenerated by re-running `build-modelfile.ts` against the committed `sample-teacher-data.jsonl` so the artifact reflects the new adapter path

**Verifiable**:

```
$ grep -rn 'gemma4-perfagent\|perfagent-smoke' packages/evals/
(no output — zero matches)
```

**Smoke run** (real Ollama, with R8/R9-fixed tool-loop disabled because smoke just runs `gemma4:e4b` directly):

```
$ EVAL_DISTILL_INPUT=.../sample-teacher-data.jsonl pnpm --filter @neuve/evals exec tsx ./scripts/distill/smoke-finetune.ts
[INFO] Smoke model created { smokeModel: 'browsing-gemma-smoke' }
[INFO] Smoke model responded { responseLength: 491 }
[INFO] Smoke model removed { smokeModel: 'browsing-gemma-smoke' }
```

End-to-end: `ollama create browsing-gemma-smoke` → generate → `ollama rm browsing-gemma-smoke`. Path verified live against real Ollama.

**Wave 5 diary**: leaving wave-5-distillation.md as historical record (it documents what wave-5 actually shipped at the time). Added a `> NOTE` pointer at the top of that file directing future readers to R11 for the canonical recipe.

**Effort**: small. No design surface. No commit required to validate (uncommitted, will commit after reviewer APPROVE per `feedback_commit_guidelines.md`).

## P2 — Strict-filter retrofit (R10 closure note tri-criterion)

R10 closure note (`docs/handover/teacher-viability/diary/r10-2026-04-30.md` §"Closure notes for the distillation wave"):

> Distillation training-data export filter must be `RUN_COMPLETED:passed AND finalState == 1.0 AND step-coverage == 1.0`, NOT just `passed`.

The two failure shapes the strict gate isolates:

1. **Premature** — Pro 3 declares done before satisfying final-state. R10 sweep had 13/20 gemini-react RUN_COMPLETED:failed plus several RUN_COMPLETED:passed cases where `finalState` scored 0.0–0.5. Status-only filter rejects RUN_COMPLETED:failed (correctly) but accepts the partial-finalState-passed cases (incorrectly — those are premature).
2. **Over-execution** — Pro keeps tooling past the success state, often to MAX_TOOL_ROUNDS. Exemplar: `calibration-2-single-nav-news`, where Pro hit all 3 STEP_DONEs then ran 2+ levels deeper. Status-only filter ACCEPTS this case; the trajectory is contaminated downstream.

Status-only filter passes (1) and (2). Tri-criterion conjunction blocks both.

### Sub-decisions (surfaced to lead before writing code)

**Decision 2A — emission seam**: chose `scripts/wave-r5-ab/build-report.ts` over `runners/trace-recorder.ts`.

Rationale: build-report is where score computation lives. `score(task, projection)` at lines 251-288 already calls `stepCoverage(...)`, `finalState(...)`, `furthestKeyNode(...)` for every (runner × task) ndjson, with the EvalTask registry in scope. Trace-recorder doesn't have an EvalTask reference at write time — wiring scoring into the recorder would couple it to the task layer and duplicate the report logic (CLAUDE.md "no duplication"). Build-report's single-pass aggregator IS the canonical scoring layer; sidecar emission slots in next to the existing `rollupsByRunnerByTask` Map population.

Cost: build-report becomes mandatory before distill:export against a new trace archive. Acceptable — the report is canonical for any sweep that gets compared.

**Decision 2B — backfill strategy**: chose retroactive (re-run build-report over R10 archive) over re-eval.

Rationale: re-running the wave-r10-pro-preview sweep wastes ~$25 + 3hr wall-clock; the existing 60 ndjson files already contain everything build-report needs (status_marker payloads, key-node URL extraction, tool-call counting). Retro path is `pnpm wave-r5-ab:report --trace-dir <wave-r10-pro-preview>` once per archive. Same one-shot covers any historical archive (`baseline/`, `post-q9-fix/`, etc.).

Both decisions match the plan's leans (P2 text "lean is build-report" + "lean is retrobackfill"). Lead acknowledged via SendMessage at session-start.

### Implementation

New file `packages/evals/src/distill/sidecar-score.ts`:
- `SidecarScore` Schema.Class with `status: "passed"|"failed"|"unfinished"`, `finalState: number`, `stepCoverage: number`.
- `SidecarScoreFromJsonString` for decode-from-JSON.
- `sidecarPathForTrace(tracePath)` pure function producing `<runner>__<taskId>.scores.json` next to the ndjson.

Filter extension at `packages/evals/src/distill/filters.ts`:
- Kept the existing 1-arg `isTraceSuccessful(events)` untouched (Wave 5 contract preserved — all 22 prior tests still pass).
- Added new `isTraceStrictlyClean(events, sidecar)` for the R11 contract:
  ```
  isTraceSuccessful(events)
    AND sidecar !== undefined
    AND sidecar.status === "passed"
    AND sidecar.finalState === 1
    AND sidecar.stepCoverage === 1
  ```
- Fail-closed on missing sidecar per plan §P2 intra-phase decision (no status-only fallback for legacy archives).

Exporter wiring at `packages/evals/src/distill/teacher-data-exporter.ts`:
- New `readSidecarScoreWith(fileSystem)` Effect.fn — `Effect.catchReason("PlatformError", "NotFound", () => Effect.succeed(undefined))` so missing sidecar surfaces as `undefined`, not an error. Other PlatformError reasons + SchemaError both `Effect.die` (unrecoverable per CLAUDE.md).
- Per-trace branch on `options.strict`: strict path reads sidecar, gates on `isTraceStrictlyClean`, distinct rejection-reason counters (`strictRejectedNoSidecar`, `strictRejectedFinalState`, `strictRejectedStepCoverage`) for sweep diagnostics. Status-only path preserves Wave 5 behavior for back-compat.

ExportOptions schema extension at `packages/evals/src/distill/types.ts`:
- New `strict: Schema.optional(Schema.Boolean)` field. Default `false` so Wave 5 fixture tests keep passing without modification.

Production script at `packages/evals/scripts/distill/export-teacher-data.ts`:
- New `EVAL_DISTILL_STRICT` env-var, `Config.boolean` with default `true`. Production exports default to strict; operators can flip to `false` for diagnostics.

Build-report emission at `packages/evals/scripts/wave-r5-ab/build-report.ts`:
- After scoring each trace, write `<runner>__<taskId>.scores.json` alongside the ndjson with the canonical SidecarScore JSON. `sidecarsWritten` counter logged at info level.

### Verifiable evidence

**Tests**: 4 new pure-function `isTraceStrictlyClean` tests (clean / premature / over-execution / missing-sidecar) + 3 new strict-mode integration tests (admit / reject finalState / reject missing sidecar) = 7 new strict-filter tests. Plan asked for 3; delivered 7 to cover both layers (pure function + exporter integration) + the explicit fail-closed-on-missing-sidecar case.

```
$ pnpm --filter @neuve/evals test
Test Files  17 passed (17)
     Tests  183 passed (183)   # was 175 pre-R11; +8 new (7 strict + 1 Path B)
```

**R10 retroactive backfill**:

```
$ pnpm --filter @neuve/evals exec tsx ./scripts/wave-r5-ab/build-report.ts \
    --trace-dir packages/evals/evals/traces/wave-r10-pro-preview \
    --output /tmp/r11-r10-retro-sidecar-report.md
[INFO] Wave R5 A:B report written { sidecarsWritten: 60 }

$ ls packages/evals/evals/traces/wave-r10-pro-preview/*.scores.json | wc -l
60
```

60 sidecars emitted (20 tasks × 3 runners — gemma-react + gemini-react + gemma-oracle-plan).

**Strict-filter pass count on R10 archive** (the plan's verifiable):

```
$ ls wave-r10-pro-preview/gemini-react__*.scores.json | \
    xargs -I {} sh -c 'cat {} | grep -q ''"status":"passed"'' && \
                       cat {} | grep -q ''"finalState":1'' && \
                       cat {} | grep -q ''"stepCoverage":1'' && \
                       basename {} .scores.json'
gemini-react__calibration-1-single-nav-python-docs
gemini-react__trivial-1-example-homepage
```

**Exactly 2/20 gemini-react traces survive** — matches R10 closure note exactly.

End-to-end strict export (all 60 R10 traces, wildcard runner filter to show full surface):

```
$ EVAL_TRACE_DIR=.../wave-r10-pro-preview EVAL_DISTILL_OUTPUT=/tmp/r11-all-runners-export.jsonl \
    EVAL_DISTILL_STRICT=true EVAL_DISTILL_RUNNER='*' EVAL_DISTILL_TEACHER=gemini-3-pro-preview \
    pnpm --filter @neuve/evals exec tsx ./scripts/distill/export-teacher-data.ts

  tracesScanned:               60
  tracesAccepted:               4    # gemini-react × 2 + gemma-react × 2
  tracesRejected:              56
  samplesWritten:               4
  strictRejectedNoSidecar:      0
  strictRejectedFinalState:    55
  strictRejectedStepCoverage:   1
```

Surviving traces:
- `gemini-react__calibration-1-single-nav-python-docs` (Pro 3 strict-clean)
- `gemini-react__trivial-1-example-homepage` (Pro 3 strict-clean)
- `gemma-react__calibration-1-single-nav-python-docs` (gemma strict-clean)
- `gemma-react__calibration-2-single-nav-news` (gemma strict-clean)

The 2/20 gemini-react number is the headline R10 stat.

## P2.1 — Teacher-only runner filter (lead-authorized library-level refactor)

Lead flagged: distillation philosophy says train on TEACHER trajectories only. Including the student's own strict-pass gemma-react traces teaches the student what it already does — near-zero gradient on already-mastered tasks. Original plan was script-level (path A as briefed); lead's follow-up moved this to the **library boundary** so the P4 training driver and any future R12 multi-sweep consumers stay runner-aware-free.

### Implementation (library-level)

- `packages/evals/src/distill/types.ts` — new `runnerFilter: Schema.optional(Schema.Array(Schema.String))` on `ExportOptions`. Allowlist semantics (empty/absent → no gating; non-empty → strict allowlist).
- `packages/evals/src/distill/teacher-data-exporter.ts` — apply allowlist inside `runExport` after `parseTraceFilename`, before task lookup. New `runnerFilterRejected` counter on the export summary log line.
- `packages/evals/scripts/distill/export-teacher-data.ts` — `EVAL_DISTILL_RUNNER` env (default `gemini-react`). Parses to a string array (`,` for multiple, `*` for "no filter"). Threads to `ExportOptions.runnerFilter`. The script no longer owns the filter logic — it's a thin env→option adapter.

### Verifiable

**Unit test** at `tests/teacher-data-exporter.test.ts`:

> `strict mode + runnerFilter: gemini-react allowlist accepts gemini-react traces and rejects gemma-react even when both pass strict tri-criterion (R11 P2.1 teacher-only default)`

Two strict-pass traces (one `gemini-react__gemini-task`, one `gemma-react__gemma-task`), each with a clean sidecar. With `runnerFilter: ["gemini-react"]`:
- `tracesAccepted: 1` (gemini only)
- `tracesRejected: 1` (gemma rejected by allowlist)
- `samples[0].metadata.runnerName === "gemini-react"`

Without `runnerFilter`:
- `tracesAccepted: 2` (both strict-clean accepted) — sanity proof that the gate is what rejects gemma, not some other path.

**End-to-end R10** (default teacher-only):

```
$ EVAL_TRACE_DIR=.../wave-r10-pro-preview EVAL_DISTILL_OUTPUT=/tmp/r11-teacher-final.jsonl \
    EVAL_DISTILL_STRICT=true EVAL_DISTILL_TEACHER=gemini-3-pro-preview \
    pnpm --filter @neuve/evals exec tsx ./scripts/distill/export-teacher-data.ts

  tracesScanned:           60     # all R10 runners scanned (no script-level pre-filter)
  tracesAccepted:           2     # gemini-react × 2 — the R10 closure number
  tracesRejected:          58     # 56 strict-rejects + 2 runner-filter-rejects
  strictRejectedFinalState:55
  strictRejectedStepCoverage:1
  runnerFilterRejected:     2     # gemma-react × 2 strict-clean but excluded
  samplesWritten:           2
  runnerFilter:    gemini-react
```

**Wildcard mode** (`EVAL_DISTILL_RUNNER='*'`) verified to fall back to 4 samples (gemini × 2 + gemma × 2) for R12 diagnostic / multi-sweep work.

Surviving traces (teacher-only default): `gemini-react__calibration-1-single-nav-python-docs` + `gemini-react__trivial-1-example-homepage`. Matches R10 closure note's headline 2/20 exactly.

184 tests now passing in `@neuve/evals` (175 prior + 9 new — 4 pure-filter, 3 strict-mode integration, 1 P2.1 runner-filter, 1 Path B conformance).

Sixth granular commit slotted into the post-APPROVE plan: `feat(distill): filter export to teacher runner only by default (EVAL_DISTILL_RUNNER)`.

**Effort**: medium. Single seam decision (build-report) + one new file (sidecar-score.ts) + one new function (isTraceStrictlyClean) + integration into existing exporter pipeline + 7 tests + one retroactive backfill run.

## P3 — Path B grammar-override conformance test

Per Wave 5 diary §"Training-sample format decision" + assessment §9, the existing exporter already emits Path B-shaped output: `assistant.toolCalls[].arguments` is a JSON string carrying the args object, and the production runtime parser at `packages/shared/src/react-envelope.ts` decodes that args object into the strict per-tool `ActionVariantArgs` union.

P3 pins this contract via a regression test. Without it, a future exporter change that mistypes `command`, emits unknown arg keys, or shape-shifts `args` would teach `browsing-gemma` to emit envelopes the production reducer rejects — the third-strike pattern from `feedback_no_test_only_injection_seams.md`.

### Implementation

New test in `packages/evals/tests/teacher-data-exporter.test.ts`: "Path B conformance: every emitted assistant.toolCalls[].arguments round-trips through parseAgentTurn as a strict ACTION envelope".

Synthetic trace fixture covering all three dispatcher tools:
- `interact { action: { command: "navigate", url: "https://example.com" } }`
- `observe { action: { command: "snapshot" } }`
- `trace { action: { command: "start", reload: true, autoStop: true } }`

For each emitted `toolCall`, the test:
1. Parses `arguments` JSON.
2. Synthesizes the wire envelope `{ _tag: "ACTION", stepId: "step-N", toolName: <name>, args: <parsed> }`.
3. Runs through `parseAgentTurn(envelope)` from `@neuve/shared/react-envelope`.
4. Asserts `instanceOf(decoded, ActionTurn)`.

The test exercises all three dispatcher branches (`InteractArgs`, `ObserveArgs`, `TraceArgs`) of the strict per-tool union. If any future emission shape drifts from canonical, the test fails at PR time.

### Verifiable

```
$ pnpm --filter @neuve/evals test -- teacher-data-exporter
... 
Path B conformance: every emitted assistant.toolCalls[].arguments round-trips through parseAgentTurn as a strict ACTION envelope (R11 P3) ✓
```

R10 strict-pass traces (calibration-1, trivial-1) export cleanly under the strict filter from P2 — both have `tool_call.name = "interact"` / `"observe"` (bare names, matching production parser) and well-formed args; the round-trip works.

The pre-existing PLAN_UPDATE round-trip test at `teacher-data-exporter.test.ts:514-624` covers the PLAN_UPDATE leg of the envelope union; P3's new test covers the ACTION leg. Together they pin both wire shapes the model is being trained to emit.

**Effort**: small. Single test addition + 3 imports.

## Pre-existing typecheck noise

Repo-wide `pnpm typecheck` fails on `@neuve/sdk` due to missing `playwright` dependency (`packages/typescript-sdk/src/perf-agent.ts:17`). This is pre-existing and unrelated to R11 — wave-5 reviewer flagged the same issue 14 days ago ("Pre-existing; unrelated to Wave 5"). `@neuve/evals` typecheck is clean.

## Pre-existing formatter drift

`pnpm --filter @neuve/evals format:check` reports 10 unrelated files needing formatting:
- `evals/wave-r5-ab.eval.ts`, `scripts/aggregate-baseline-run.ts`
- `src/runners/{gemini-agent,gemini-react-loop,gemini,real}.ts`
- `tests/{gemini-live-smoke,gemini-react-loop,plan-decomposer,wave-r5-ab-aggregate}.test.ts`

`git diff HEAD` against those files is empty — drift is on disk pre-R11. Same Wave 5 M5 pattern (formatter normalizes whole packages, sibling files drift). Format-check on R11-touched files only is clean (10/10). Reviewer can confirm via:

```
$ pnpm --filter @neuve/evals exec vp fmt --check \
    src/distill/sidecar-score.ts src/distill/filters.ts \
    src/distill/teacher-data-exporter.ts src/distill/types.ts \
    scripts/distill/build-modelfile.ts scripts/distill/smoke-finetune.ts \
    scripts/distill/export-teacher-data.ts \
    scripts/wave-r5-ab/build-report.ts \
    tests/teacher-data-exporter.test.ts tests/modelfile-builder.test.ts
All matched files use the correct format.
```

Following Wave 5's resolution: not reverting (formatter wins; HEAD shape itself is non-conforming), surfacing as scope-hygiene disclosure here. Lead's call on whether to fold a `chore(evals): revert pre-existing formatter drift` commit into R11's stack like Wave 5 did.

## What ships uncommitted

All P1+P2+P2.1+P3 code is on disk. No commits yet — per `feedback_commit_guidelines.md`, granular commits land after reviewer APPROVE. Commit plan when approved:

1. `chore(distill): rename gemma4-perfagent → browsing-gemma`
2. `feat(evals,distill): emit sidecar score files for distillation strict filter`
3. `feat(distill): tri-criterion strict filter (passed + finalState + stepCoverage)`
4. `feat(distill): filter export to teacher runner only by default (EVAL_DISTILL_RUNNER)`
5. `test(distill): pin Path B JSON envelope conformance via parseAgentTurn round-trip`
6. `docs(distillation-pipeline): R11 P1-P3 diary`

Plus the 60 sidecar JSON files at `packages/evals/evals/traces/wave-r10-pro-preview/*.scores.json` — these are derived artifacts (re-runnable from the ndjson via build-report). Likely decision: gitignore them. Lead's call.

## Surfaced to lead

P1+P2+P3 done. Sub-decisions 2A (build-report seam) and 2B (retroactive backfill) sent to team-lead at session-start; proceeded against my proposed design in parallel given the rationale was well-grounded and matched the plan's leans.

Strict-filter pass count = 2/20 on the gemini-react lane. Plan gate verified. Path B conformance test pins the wire format. Build-report becomes the canonical sidecar emitter. Smoke-finetune confirms the rename runs end-to-end against real Ollama.

Awaiting reviewer APPROVE before claiming Task #3 (P4-P7).

---

## P4 — MLX-LM training driver (in flight, BLOCKED)

### Sub-probe-zero — MLX-LM availability + Gemma 4 module presence (PASS)

```
$ brew install mlx-lm
==> Caveats / mlx-lm 0.31.3 (bottled), HEAD installed cleanly
$ which mlx_lm.lora
/opt/homebrew/bin/mlx_lm.lora                     # symlinked from /opt/homebrew/Cellar/mlx-lm/0.31.3/libexec/bin
$ mlx_lm.lora --help                              # exits 0; full flag surface present
$ ls /opt/homebrew/Cellar/mlx-lm/0.31.3/libexec/lib/python3.14/site-packages/mlx_lm/models/ | grep gemma
gemma.py
gemma2.py
gemma3_text.py
gemma3.py
gemma3n.py
gemma4.py            ← multimodal (Gemma4ForConditionalGeneration)
gemma4_text.py       ← text-only (model_type: gemma4_text)
recurrent_gemma.py
```

P4 zero gates pass:
- `mlx_lm.lora` binary on PATH ✓
- MLX-LM ships explicit Gemma 4 model classes (`gemma4.py`, `gemma4_text.py`) ✓
- `mlx-community/gemma-4-e4b-it-bf16` checkpoint published on HF ✓

### Sub-probe-one — actual training on production target (FAIL: BLOCKER)

Probe-one is the integration test the plan §"Risk areas" #5 calls for: "verify MLX-LM Gemma 4 first-time integration on a known-good HF base model checkpoint before wiring full pipeline." Without this we'd write the Effect wrapper against an architecture the loader doesn't accept.

```
$ mkdir -p /tmp/r11-mlx-probe/data && \
    cp /tmp/r11-teacher-final.jsonl /tmp/r11-mlx-probe/data/train.jsonl && \
    touch /tmp/r11-mlx-probe/data/{valid,test}.jsonl
$ mlx_lm.lora --model mlx-community/gemma-4-e4b-it-bf16 --train \
    --data /tmp/r11-mlx-probe/data --fine-tune-type lora --num-layers 4 \
    --batch-size 1 --iters 2 --learning-rate 5e-5 --max-seq-length 1024 \
    --adapter-path /tmp/r11-mlx-probe/adapter --seed 42

Loading pretrained model
Fetching 11 files: 100%
Traceback (most recent call last):
  ...
  File ".../mlx/nn/layers/base.py", line 185, in load_weights
    raise ValueError(f"Received {num_extra} parameters not in model: \n{extras}.")
ValueError: Received 54 parameters not in model:
  language_model.model.layers.{24..41}.self_attn.{k_norm,k_proj,v_proj}.weight
```

54 extra weights = 18 layers × 3 keys. Layers 24-41 == the kv-shared layers (`text_config.num_kv_shared_layers: 18` in the checkpoint config). MLX-LM 0.31.3's `gemma4_text.py` only allocates K/V projections for `num_hidden_layers − num_kv_shared_layers = 24` non-shared layers; the published checkpoint includes redundant K/V weights for the shared layers anyway. `load_weights(strict=True)` (MLX-LM default) bails.

### Why this is a BLOCKER (per plan §"Risk areas" #5)

> If either sub-probe-zero fails, surface to lead immediately — that's a P4/P5 blocker, not a soldier-on situation.

The mismatch is upstream: `mlx-community/gemma-4-e4b-it-bf16` was published with extra weights MLX-LM 0.31.3 can't ingest. Three observations bound the search:

1. **HF `transformers` loads the same checkpoint cleanly** (different loader, tolerant of extra keys). MLX-LM is the bottleneck, not the checkpoint.
2. **`deadbydawn101/gemma-4-E4B-opus-reasoning-claude-code-lora`** exists on HF as a Gemma 4 E4B LoRA adapter — proving SOMEONE successfully fine-tuned. They likely used a different MLX-LM commit, a custom config, or upstream `transformers` instead.
3. **No quick `--strict-load false` flag** is wired through `mlx_lm.lora`'s CLI; the loader call is fixed in `mlx_lm/utils.py:415`.

Mitigation paths captured in `docs/handover/distillation-pipeline/probes/p4-mlx-lm-gemma4-incompat.md`:

| Path | Description |
|---|---|
| **A** | Switch base to Gemma 3n E4B — MLX-LM has battle-tested `gemma3n.py`. **CAVEAT**: deployed model becomes `browsing-gemma-3n` (Gemma 3n base), not `browsing-gemma` (Gemma 4 base). Deviates from `project_target_model_gemma.md`. |
| **B** | Wait for MLX-LM upstream fix. Out of R11 scope. |
| **C** | Preprocess checkpoint to strip redundant weights. Brittle. |
| **D** | Earlier MLX-LM via venv. Speculative. |
| **E** | HF `transformers` + `peft` directly. Loses Apple Silicon speedup; deviates from plan §"Locked decisions" #4. |
| **F** | Punt P4 training driver to R12. R11 ships P1+P2+P2.1+P3+P5+P6+P7 plumbing. **DoD #8 (behavioral floor) becomes untestable** in R11. |

### Surfaced to lead

Surfaced 2026-04-30 with the artifact link + matrix + three explicit decision questions (path / rename / R11 DoD scope). Awaiting direction before proceeding.

### Effect wrapper status (uncommitted, ready for any path)

`packages/evals/scripts/distill/train.ts` is written + typechecks clean. Base-model-agnostic via `EVAL_DISTILL_BASE_MODEL_PATH` env so any of paths A/B/C/D/F can re-use the wrapper without code change. Path E (`peft`) would need a new wrapper.

`packages/evals/tests/distill-train.test.ts` is written + typechecks clean. Structured-error test runs on any platform (asserts `JsonlReadError`/`JsonlEmptyError` fires before MLX-LM spawn). Live smoke is gated via `it.skipIf(!isMlxLmAvailable() || !isHfModelCached(SMOKE_BASE_MODEL))` — uses `Qwen2.5-0.5B-Instruct-bf16` as a tiny known-good MLX checkpoint to exercise the path without burning multi-gigabyte CI bandwidth.

`packages/evals/package.json` has `distill:train` script wired.

**Held uncommitted** until lead direction lands — committing now would ship a wrapper pointing at a known-broken default base model.

### Disk state

`~/.cache/huggingface/hub/models--mlx-community--gemma-4-e4b-it-bf16` is ~13 GB local. Will purge if lead picks A/B/C/E/F (B/C/E/F don't need it; A switches to Gemma 3n E4B which is a separate cache entry).

---

## Path E authorized + executed (2026-05-01)

User authorized **E2** (HF+peft training + browsing-gemma-react served via `llama-server --lora`). Plan amendment landed at `4458ed88` flipping locked decision #4 from "MLX-LM only" → "HF transformers + peft" and adding new locked decision #9 (runtime fork: browsing-gemma-react → llama-server, all other lanes stay on Ollama).

### P4 — HF+peft training driver

`packages/evals/scripts/distill/peft_train.py` (Python) + `packages/evals/scripts/distill/train.ts` (Effect wrapper) + `packages/evals/tests/distill-train.test.ts` (live smoke). Tagged errors: `HFPeftUnavailableError`, `JsonlReadError`, `JsonlEmptyError`, `JsonlValidationError`, `HFPeftTrainFailedError`, `AdapterArtifactMissingError`. New env knob `EVAL_DISTILL_TRAIN_TARGET_MODULES` (regex or comma-list; default Gemma 4 language_model regex).

Live smoke: trains a 1-iter LoRA against `HuggingFaceTB/SmolLM2-135M-Instruct` cached locally. Commits `d126ac34` + `79606f49`.

### P5 — GGUF conversion

`packages/evals/scripts/distill/convert-gguf.ts` Effect wrapper around llama.cpp `convert_lora_to_gguf.py`. Validates 4 inputs upfront (script, adapter dir, base-model HF cache snapshot, gguf-py has `MODEL_ARCH.GEMMA4`).

Documented gotcha: published `gguf` PyPI lacks `MODEL_ARCH.GEMMA4`; install gguf-py from local llama.cpp clone via `pip install -e <llama.cpp>/gguf-py`. Commit `305f686a`.

### P5.5 — LlamaServerClient

`packages/evals/src/runners/llama-server-client.ts` ServiceMap.Service. Two operating modes: spawn-per-eval (Effect.acquireRelease around `llama-server` subprocess) or pre-running instance (via `PERF_AGENT_LLAMA_SERVER_URL`). OpenAI `/v1/chat/completions`. Returns `content` + `reasoning_content` separately. Live smoke: 3.25s wall-clock with cached HF base. Commit `bbce0793`.

### P6 — OllamaApiAdapter HTTP proxy + runner wiring

**Architecture choice**: HTTP proxy (in evals package), NOT a TS-level OllamaClient subclass. Reason: `@neuve/local-agent`'s `createOllamaClient()` factory has no Layer/ServiceMap injection seam; touching local-agent would trip the build-cache trap. The proxy lives entirely in evals; production code untouched.

`packages/evals/src/runners/ollama-api-adapter.ts` ServiceMap.Service. Endpoints: `GET /` (health), `GET /api/version`, `GET /api/tags` (model listing — required by `@neuve/agent` preflight), `POST /api/chat` (translates Ollama → llama-server `/v1/chat/completions`).

Sub-probe-one (live, via `parseAgentTurnFromString`): adapter receives Ollama-shape `/api/chat` with AgentTurn `format` → translates to llama-server `response_format: json_schema` → returns single-ndjson Ollama chunk → content round-trips through `parseAgentTurn` as valid envelope. ✓

Three adapter fixes landed mid-flight in commit `539effd7` (post-T4 review — reviewer caught the gap; the changes were on disk during the sweep but were never committed until after T4 flagged it):
1. **Default port flipped to 0** (OS picks ephemeral) so evalite's parallel-task model doesn't EADDRINUSE-collide. `server.address().port` reads back the bound port.
2. **`/api/tags` endpoint** added so local-agent's `AcpAdapter.layerLocal` preflight finds the configured model name.
3. **`EVAL_OLLAMA_ADAPTER_MODEL_NAME` config** added to thread the served model name into `/api/tags`. Default `browsing-gemma`.

Runner wiring at `packages/evals/src/runners/gemma.ts`: extends `GemmaRunnerOptions` with `runtime: "ollama" | "llama-server"` + `adapterPath?: string`. When runtime is llama-server, `run(task)` boots `OllamaApiAdapter` scoped to the task and overlays `PERF_AGENT_OLLAMA_URL` → adapter URL via ConfigProvider. Production code untouched. Commits `bbce0793` (LlamaServerClient), `f28eab4f` (initial OllamaApiAdapter), `7740ea4e` (gemma.ts runtime fork + eval-lane registration), `539effd7` (port + /api/tags + model-name config fixes).

### P7 — wave-r5-ab report 4th column + delta tables

`packages/evals/scripts/wave-r5-ab/build-report.ts` extended:
- Per-task table header now built dynamically from `RUNNER_NAMES` so adding a runner lights up the column without code change.
- Two new pairwise comparisons: `gemma-react vs browsing-gemma-react` (DoD #8 regression check) and `browsing-gemma-react vs gemini-react` (R11 lift ceiling).

Commit `7740ea4e`.

### Sweep execution

**Browsing-gemma adapter trained** on R10 strict-pass dataset (calibration-1 + trivial-1, 2 samples). Hyperparams: rank 8, 1 epoch, lr 5e-5, batch_size 1, max_seq_length 1024 (tightened from default 4096 — M4 Pro 32GB MPS soft-cap is 30GB; default 4096 OOM'd on the cross-entropy loss step at 262K vocab × seq × bf16 logits). `PYTORCH_MPS_HIGH_WATERMARK_RATIO=0.0` allowed unified-memory headroom past the soft cap. Output: 17MB Safetensors → 9MB GGUF.

**`llama-server` started** with `-hf ggml-org/gemma-4-E4B-it-GGUF:Q4_K_M --lora browsing-gemma.gguf --port 8090 --no-webui --n-gpu-layers 999`. Healthy on `/health`.

**Wave-r11 trace dir** seeded with R10's gemma-react/gemini-react/gemma-oracle-plan ndjsons + sidecars (those lanes are R10-baseline-equivalent — R8/R9/R10 invariants stable, no need to re-pay $25 + 90min sweep cost). Then ran browsing-gemma-react ONLY against the full 20-task suite (`EVAL_R5_SKIP_RUNNERS=gemma-react,gemini-react,gemma-oracle-plan`) — completed in ~57 min wall-clock starting 01:00.

### DoD verdicts

Report at `docs/handover/harness-evals/baselines/wave-r11-browsing-gemma.md` (auto-generated via `pnpm wave-r5-ab:report --trace-dir wave-r11-browsing-gemma --output ...`).

| # | Gate | Threshold | Result | Pass? |
|---|---|---|---|---|
| 1 | Identity reconciliation | grep zero | 0 hits | ✓ |
| 2 | Strict-filter correctness | 22 prior + 4 strict-filter tests pass; 2/20 R10 gemini-react accepted | 188+/188+ tests, 2/20 confirmed | ✓ |
| 3 | Path B format conformance | new test pins parseAgentTurn round-trip | live test passes | ✓ |
| 4 | HF+peft training driver | adapter file produced; live smoke passes | 17MB Safetensors saved, smoke green | ✓ |
| 5 | GGUF conversion | `.gguf` produced; loadable | 9MB GGUF, llama-server loads + serves | ✓ |
| 5.5 | llama-server runtime | non-empty response via `--lora` | 3.25s smoke + curl probe live | ✓ |
| 6 | End-to-end local pipeline | single command sequence exits 0 | `distill:export → distill:train → distill:convert-gguf` chain green | ✓ |
| 7 | Eval lane | 4 runner columns | wave-r11 report has 4 columns | ✓ |
| 8 | **Behavioral floor** | browsing-gemma step-cov ≥ gemma-react − 0.05 | 0.237 vs 0.307 − 0.05 = 0.257 → **−0.020 outside band** | ✗ |
| 9 | R8/R9/R10 invariants | empty-content 0/20, schema-invalid ≤5/20 on gemma-react | inherited from R10 (gemma-react traces are R10 baseline copies, R8/R9/R10 sweep invariants stable) | ✓ |

**Headline aggregate scores** (mean step-coverage, full 20-task sweep × 4 runners):

| Runner | Mean step-cov | Mean final-state | Pass | Fail | Incomplete |
|---|---|---|---|---|---|
| gemma-react | 0.307 | 0.150 | 5 | 2 | 13 |
| gemini-react | 0.473 | 0.100 | 7 | 13 | 0 |
| gemma-oracle-plan | 0.258 | 0.000 | 0 | 0 | 20 |
| **browsing-gemma-react** | **0.237** | **0.100** | **5** | **2** | **13** |

### DoD #8 detail — what we lost

Per-task deltas vs base-gemma (Δ ≥ 0.20):

**browsing-gemma WINS**: journey-2 (cov 0 → 0.20), journey-4 (cov 0 → 0.20), journey-5 (cov 0 → 0.25), moderate-1 (furthest 0.333 → 0.667). 4 wins on journey-* bridge tasks where base-gemma scored zero — same task family gemini Pro 3 also wins on, suggesting the trace pattern transfers partially.

**browsing-gemma LOSSES**: calibration-2 (cov 1.00 → 0.00, **−1.00**), journey-6 (cov 0.80 → 0.00, **−0.80**), journey-8 (cov 0.25 → 0.00, **−0.25**).

The calibration-2 + journey-6 losses are catastrophic narrowing: the LoRA over-specializes on the 2 trivial-shape training samples and misfires on tasks where base-gemma had partial competence. This is exactly the risk plan §"Risk areas" #4 flagged.

**Net**: −0.07 mean step-coverage vs base-gemma. Outside the −0.05 floor by 0.02. R11 plan calls this "the LoRA broke the base-gemma capability rather than just adding nothing." Honest read: the 2-trace LoRA broke gemma slightly more than it added value. Expected per data-scarcity gate.

R12's planned scope (multi-sweep capture for dataset growth, trajectory cleanup to recover passed-only stragglers, oracle-plan trajectory mixing) directly addresses the catastrophic narrowing — the data-scarcity ceiling is the actual blocker, not the pipeline.

### Final commit plan after T4 reviewer APPROVE

Committed this session (16 commits total, including post-T4-review fix):

1. `40da2cce` chore(distill): rename gemma4-perfagent → browsing-gemma
2. `644c2c5f` feat(evals,distill): emit sidecar score files
3. `3eb6649f` feat(distill): tri-criterion strict filter
4. `d388f21b` test(distill): pin Path B JSON envelope conformance
5. `f69f5b35` feat(distill): default export to teacher-only
6. `6ba5cc4d` docs(distillation-pipeline): R11 P1-P3 diary
7. `4458ed88` docs(distillation-pipeline): plan amendment for E2
8. `d126ac34` feat(distill): HF+peft training driver
9. `79606f49` test(distill): live HF+peft training smoke
10. `305f686a` feat(distill): GGUF conversion wrapper
11. `bbce0793` feat(evals): LlamaServerClient
12. `f28eab4f` feat(evals): OllamaApiAdapter
13. `7740ea4e` feat(evals): browsing-gemma-react runner lane + report 4th column
14. `8381a327` docs(harness-evals): wave R11 browsing-gemma baseline
15. `f01e2a37` docs(distillation-pipeline): R11 P4-P7 diary closure
16. `539effd7` fix(evals): OllamaApiAdapter — ephemeral port + /api/tags for parallel sweep + preflight (post-T4-review fix; the changes were on disk during the sweep but had never been committed; reviewer flagged the gap)

A subsequent commit lands this diary correction (the §P6 misattribution).

### Runtime artifacts (gitignored / scratch)

- `/tmp/r11-pathE/venv/` — Python venv with peft + transformers + torch + gguf-py
- `/tmp/r11-pathE/llama.cpp/` — sparse-checkout for `convert_lora_to_gguf.py` + `gguf-py`
- `packages/evals/data/distill/out/teacher-data.jsonl` — 2-sample R10 teacher data (gitignored)
- `packages/evals/data/distill/out/adapter/` — peft Safetensors adapter (gitignored)
- `packages/evals/data/distill/out/browsing-gemma.gguf` — GGUF for `--lora` (gitignored)
- `packages/evals/evals/traces/wave-r11-browsing-gemma/` — 80 ndjson + 80 .scores.json (gitignored under `traces/`)

### R12 inheritances

Per plan amendment §"Out of scope":
- **Ollama LoRA reconvergence**: when Ollama lands LoRA inference upstream, drop the runtime fork.
- **MLX-LM Gemma 4 fix retry**: when MLX-LM lands kv-shared K/V loader fix, evaluate switching back from HF+peft for Apple Silicon speedup at scale.
- **DoD #8 capability-lift gate**: R12 owns growing the strict-pass dataset above 2 traces (multi-sweep capture, trajectory cleanup, oracle-plan trajectory mixing) so the LoRA has enough signal to OVERCOME the catastrophic-narrowing regression we measured.

R11 ships infrastructure end-to-end; the −0.07 step-cov regression is data-scarcity-bound and IS the R12 problem statement.
