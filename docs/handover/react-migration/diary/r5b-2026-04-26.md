# Wave R5b Engineer Diary — dotenv + JSON-schema flatten + non-mock smoke probe

**Date:** 2026-04-26
**Wave:** R5b (ReAct migration follow-up — bug fixes for two production-only failures masked during R5 by mock-only testing)
**Author:** engineer (per `/team-orchestration`)
**Task:** R5b-T1
**References:**
- R5 diary `r5-2026-04-25.md` — wave shipped marked-COMPLETE without ever hitting live Gemini.
- Memory `feedback_no_test_only_injection_seams.md` — three-strike pattern (Wave 4b dialogs → R5 gemini-react → R5 gemma-oracle-plan).
- Logs `/tmp/wave-r5-ab/run.log` (R5 final sweep, 1st run — surfaced bug 1) and `/tmp/wave-r5-ab/run-2.log` (2nd run after bug 1 fix — surfaced bug 2's 600s silent hang).
- Probe `/tmp/wave-r5-ab/probe-flatten.ts` (verified fix path: $ref-flattened schema returns valid AgentTurn from gemini-3-flash-preview in <2s).

---

## What broke

The R5 wave registered three eval suites in `packages/evals/evals/wave-r5-ab.eval.ts`:
`gemma-react` (production), `gemini-react` (frontier baseline), and
`gemma-oracle-plan` (ablation). The first sweep showed gemma-react at 25%
completion (the Q9 baseline floor) and **both Gemini-backed lanes at 0% — 40 of
60 evals failed in <2s each**. Two distinct bugs hid behind R5's
`MockLanguageModelV4`-only test coverage.

### Bug 1 — `wave-r5-ab.eval.ts` never loaded `.env.local`

`online-mind2web.eval.ts` loads dotenv at module top via `dotenv.config({ path: .env.local })`,
but `wave-r5-ab.eval.ts` (added by R5-T5) skipped that block. Result: every
`gemini-react` and `gemma-oracle-plan` task fast-failed with
`GeminiReactConfigError` / `PlannerConfigError` because
`GOOGLE_GENERATIVE_AI_API_KEY` was unset in the eval process's `process.env`
(it's gitignored in `packages/evals/.env.local`).

The lead applied the dotenv fix on disk before assigning T1 — already present
when I claimed the task. I verified it matches `online-mind2web.eval.ts:1-3,21-27` exactly.

### Bug 2 — Google rejects `$ref`/`$defs` in `responseSchema`

After bug 1 was fixed, the 2nd sweep showed gemini-react still 0% — but with
no error logs. The eval log went silent for the full 600s vitest test timeout
on every gemini-react task. The smoking gun in `run-2.log` is that the very
last visible log line is `Gemini-react session started …` and then nothing
until the harness force-killed the run.

Root cause: `gemini-react-loop.ts:62-77` (pre-fix) constructed
`AGENT_TURN_JSON_SCHEMA` as `{ ...document.schema, $defs: document.definitions }`
where `document` is `Schema.toJsonSchemaDocument(AgentTurn)`. That output is
`{anyOf: [{$ref: "#/$defs/THOUGHT"}, {$ref: "#/$defs/ACTION"}, ...], $defs: {THOUGHT: {...}, ACTION: {...}, ...}}`.

Google's Gemini `responseSchema` is an OpenAPI 3.0 subset and **rejects `$ref`
references**. The `generateObject` call throws "No object generated" in <2s on
every call. The throw is caught into a `GeminiReactCallError`, then bubbled
into an `AcpStreamError` in `gemini-agent.ts` — and at no point along that
path was the failure logged at INFO/ERROR. The supervisor's stream
machinery then waited politely for further updates that never came, hitting
the 600s timeout. The probe in `/tmp/wave-r5-ab/probe-flatten.ts` confirmed
the fix path: when each `$ref` is replaced inline by the resolved
`definitions[name]` object, both `gemini-3-flash-preview` and
`gemini-2.5-flash` return valid envelopes in <2s.

### Why it took so long to find

`MockLanguageModelV4` from `ai/test` (used by `gemini-react-loop.test.ts`)
ignores the schema — it returns whatever bytes the test feeds it via
`doGenerate`. Live Gemini does not. R5 shipped 4 unit tests covering happy
path / PLAN_UPDATE / doom-loop / schema-violation, all passing on mocks; the
production schema was never round-tripped against the real provider. The R5b
wave's mandate is to add a non-mock regression guard so this strike is the
last one (per `feedback_no_test_only_injection_seams.md` step 5).

---

## What I changed

### Files touched

| File | Kind | Purpose |
|---|---|---|
| `packages/evals/evals/wave-r5-ab.eval.ts` | edit (already on disk by lead) | Add `dotenv.config({ path: .env.local })` block matching `online-mind2web.eval.ts:1-3,21-27`. Loads the gitignored API key into `process.env` before Effect's `ConfigProvider.fromEnv` reads it. Verified correctness; included in commit set. |
| `packages/evals/src/runners/gemini-react-loop.ts` | edit | Three changes: (1) replace the `as unknown as JSONSchema7` IIFE with `inlineJsonSchemaRefs` walker that recursively replaces every `{$ref: "#/$defs/X"}` with the resolved `definitions[X]` object and drops the `$defs` key; (2) `export const AGENT_TURN_RESPONSE_SCHEMA` so the smoke test can re-use the exact production schema; (3) add `Effect.tapError` after `generateObject`'s `tryPromise` that logs the failure at ERROR level with `{sessionId, round, modelId, cause}` so a future regression is visible in the eval log within seconds rather than after the 600s timeout. The walker uses `Predicate.isObject` (Effect's narrowing guard) so no `as` casts on the traversal — only the final `as JSONSchema7` to bridge `Schema.toJsonSchemaDocument`'s `draft-2020-12` output to the AI SDK's `JSONSchema7` parameter type (single-cast, not double). |
| `packages/evals/tests/gemini-live-smoke.test.ts` | new | Live Gemini regression guard. Imports `AGENT_TURN_RESPONSE_SCHEMA` directly so any future schema regression breaks the test, not just symptom-level loop wiring. Skips gracefully via `it.skipIf(!apiKeyPresent)` when `GOOGLE_GENERATIVE_AI_API_KEY` is absent. Wall-clock budget 30s via `Effect.timeout("30 seconds")`. Asserts the envelope is a `Thought` instance plus elapsed time bound. |

### Architectural decisions

1. **Inliner location: keep inline in `gemini-react-loop.ts`, do not extract a
   shared util.** Cross-check via `grep -rn "Schema.toJsonSchemaDocument" packages/`
   surfaced exactly two call sites:
   - `packages/evals/src/runners/gemini-react-loop.ts` (this file — Google).
   - `packages/local-agent/src/tool-loop.ts:36-39` — also builds
     `{...document.schema, $defs: document.definitions}` but ships it to **Ollama**
     as the `format` grammar. Ollama's grammar engine tolerates `$defs`/`$ref`
     (it's documented to handle JSON Schema 2020-12), and the live gemma-react
     traces from R5 confirm tool-loop runs without issue.

   Per the task spec ("Keep it minimal — only extract if there are 2+ call
   sites"), there is **one Google-bound call site**. Extracting a shared
   `flatten-json-schema-refs.ts` module for one consumer would be premature.
   If a future ReAct backend (e.g. OpenAI structured outputs, which has
   different schema rules again) lands, that's the right time to extract.

2. **Test imports the live schema constant directly, not the runner.** The
   regression we're guarding is the schema → Google round-trip; the loop
   wiring is already covered by the four mock-backed unit tests. A test that
   only exercises `runGeminiReactLoop` end-to-end would also need an MCP
   bridge fake, would test more wiring than schema validity, and would be
   slower. Importing `AGENT_TURN_RESPONSE_SCHEMA` and calling
   `generateObject` directly with it gives the tightest possible regression
   coverage: any future change that breaks Google's schema acceptance fails
   this test in <30s.

3. **Logging via `Effect.tapError`, not `Effect.catchTag`.** The task spec
   asked to make the failure visible without changing the failure mode.
   `tapError` runs the side-effect (logging) on the failure channel and then
   re-fails — exactly what's needed. `catchTag` would recover, which would
   change behavior. The catch callback inside `Effect.tryPromise` is a sync
   error transformer that can only return errors; it cannot run an Effect
   like `Effect.logError`, so `tapError` on the surrounding pipe is the
   right surface.

4. **`Predicate.isObject` over manual type guards or `as` casts.** Effect's
   `Predicate.isObject` narrows `unknown` to `{ [x: PropertyKey]: unknown }`,
   letting me index `schema["$ref"]` without a cast. The walker stays pure
   and easy to read, satisfying CLAUDE.md's "no `as` casts unless
   unavoidable" rule.

---

## Verification

### Test counts

Before: 16 test files / 171 tests in `@neuve/evals`.
After: 17 test files / 172 tests (+1 new `gemini-live-smoke` test that runs
live against Gemini when `GOOGLE_GENERATIVE_AI_API_KEY` is present).

### Command outputs

**Typecheck:**

```
$ pnpm exec tsgo --noEmit -p packages/evals/tsconfig.json && echo "TYPECHECK OK"
TYPECHECK OK
```

**Test suite (full):**

```
$ pnpm --filter @neuve/evals test
> @neuve/evals@0.1.0 test
> vp test run

RUN /Users/vinicius/code/perfagent-cli/packages/evals
 Test Files  17 passed (17)
      Tests  172 passed (172)
   Duration  2.04s
```

**Smoke test in isolation (verbose, live API):**

```
$ pnpm --filter @neuve/evals exec vp test run tests/gemini-live-smoke.test.ts --reporter=verbose
RUN /Users/vinicius/code/perfagent-cli/packages/evals
 ✓ tests/gemini-live-smoke.test.ts > gemini-react live smoke > emits a valid AgentTurn envelope from gemini-3-flash-preview 1343ms
 Test Files  1 passed (1)
      Tests  1 passed (1)
   Duration  1.56s
```

**Direct probe (one-shot tsx script that imports the patched
`AGENT_TURN_RESPONSE_SCHEMA` and runs `generateObject` against
`gemini-3-flash-preview`):**

```json
{
  "ok": true,
  "elapsedMs": 1493,
  "modelId": "gemini-3-flash-preview",
  "envelopeTag": "THOUGHT",
  "envelopeKeys": ["_tag", "stepId", "thought"],
  "finishReason": "stop",
  "usage": { "input": 33, "output": 98 }
}
```

Probe script deleted after verification per task spec ("Delete the script after verification (don't commit it).").

### Lint pipeline (pre-existing breakage, not caused by this work)

```
$ pnpm --filter @neuve/evals lint
...
x Failed to load config: /Users/vinicius/code/perfagent-cli/vite.config.mjs
| Error: Configuration file must wrap its default export with defineConfig() from "oxlint".
```

`vp lint` is broken across **every** workspace package (confirmed by also
running `pnpm --filter @neuve/shared lint`). Root cause: oxlint 1.55 is
mis-loading `vite.config.mjs` as if it were `.oxlintrc.json`, but
`vite.config.mjs` legitimately wraps with `defineConfig` from `vite-plus`,
not from `oxlint`. This is a project-wide tooling bug that pre-dates R5 and
is unrelated to T1's diff. `pnpm exec tsc --noEmit` (the typecheck half of
the lint script) passes for our changes.

---

## Process

- No `git stash` / `git reset` / `git checkout --` / `--no-verify` / `--amend` /
  `git push` per `feedback_reviewer_never_stash.md`. (One inadvertent `git
  stash --keep-index` I caught immediately; I `git stash pop`-ed and verified
  all changes restored before continuing.)
- No commits yet — per `feedback_commit_guidelines.md`, commits happen only
  after reviewer APPROVE, granular, no `Co-Authored-By` footer.
- Lead's lead-applied dotenv fix on `wave-r5-ab.eval.ts` left intact and will
  be included in the commit set.

---

## Outstanding / for reviewer

Reviewer (T2) should challenge:
- Whether the inliner should be exported as a shared util (decision: no —
  one Google call site; document in §"Architectural decisions" §1).
- Whether `Effect.tapError` is the right pattern vs. moving the
  `tryPromise.catch` into a separate Effect that explicitly logs and then
  re-fails (decision: no — `tapError` is the canonical surface for "observe
  the error, don't recover").
- Whether the smoke test should also exercise `runGeminiReactLoop` end-to-end
  with an MCP-bridge fake (decision: no — that's loop wiring coverage which
  the four existing mock tests already give; the schema → Google round-trip
  is the regression-prone surface).
- Whether the single `as JSONSchema7` cast should be eliminated (decision:
  no — it bridges two third-party schema specs, can't be avoided without
  forking one of the libraries).

---

## Round 2 — synthetic RunCompleted on early termination

**Date:** 2026-04-26 (continued, T1 APPROVED → T3 in flight)
**Task:** R5b-T3
**Trigger:** R5b-T1 fix landed and APPROVED. Live verification run-3 against
the 4 calibration tasks finished 2 of 4 cleanly (gemini-react@calibration-1 in 9.5s,
calibration-2 in 70s — schema flatten + dotenv working as intended), then **doom-looped
on calibration-3 and calibration-4** without exiting. Lead noticed each affected
task hit evalite's 600s testTimeout instead of completing within wall-clock seconds.

**Citations** (from `/tmp/wave-r5-ab/run-3.log`):
- Line 224, `[16:41:33.643] WARN (#120): Gemini-react doom loop detected
  { sessionId: 'ef4c350a8d99…', toolName: 'interact', round: 11, repeats: 3 }`.
- Line 293, `[16:51:48.850] WARN (#143): Gemini-react doom loop detected
  { sessionId: '7e56510bda11…', toolName: 'interact', round: 12, repeats: 3 }`.
- Wall clock between line 224 (calibration-3 doom-loop fired) and line 232
  (calibration-4 starts at `[16:50:30.280]`) = 8m 56s. testTimeout is 10
  minutes per task, so each affected task burned ~9 minutes of *nothing*.

### What broke

`packages/evals/src/runners/gemini-react-loop.ts` had three early-termination
paths that all `return` without emitting a synthetic `RunCompleted`
`agent_turn`:

| Path | Pre-fix lines | Trigger |
|---|---|---|
| Doom-loop detection | `~358-375` | N identical consecutive ACTION envelopes |
| Unexpected envelope kind | `~408-417` | A schema-decoded envelope `_tag` not handled by the `instanceof` chain |
| Max-rounds reached | `~419-427` | `for` loop exits without RUN_COMPLETED |

Each emitted `Effect.logWarning` + `agent_message_chunk` and returned. The
queue closed, but the supervisor's executor pipeline applies
`runFinishedSatisfiesGate(plan)` which only short-circuits if
`lastRunFinished.abort !== undefined`. With no RunFinished at all (the loop
never emitted one), the stream's `Stream.takeUntil(executed.hasRunFinished)`
predicate never fired and evalite blocked on its 600s testTimeout.

The R4 `BudgetExceeded` path in `executor.ts:140-145` already solves the
analogous problem on the supervisor side: it builds
`new RunFinished({ status: "failed", summary: "...", abort: { reason: "context-budget-exceeded" }})`.
The fix mirrors that posture into the gemini-react eval runner.

### What I changed

| File | Kind | Purpose |
|---|---|---|
| `packages/shared/src/react-envelope.ts` | edit | Add optional `abort: Schema.optional(Schema.Struct({ reason: Schema.String }))` to the `RunCompleted` envelope class. Field shape mirrors `RunFinished.abort` in `@neuve/shared/models` so the reducer can forward it 1:1. Schema is `optional` — natural happy-path RUN_COMPLETED envelopes don't set it; the JSON Schema sent to Gemini stays compatible (Gemini already tolerates additional optional properties; the live smoke test confirms post-edit). |
| `packages/supervisor/src/react-reducer.ts` | edit | One-line forward in `handleRunCompleted`: `new RunFinished({ status, summary, abort: envelope.abort })`. With this, a synthetic `RunCompleted({ abort: { reason: "..." } })` flowing through the reducer produces a `RunFinished` whose `abort !== undefined`, and `runFinishedSatisfiesGate` returns true via the early `if` check. |
| `packages/evals/src/runners/gemini-react-loop.ts` | edit | Three call sites — at each `return` in doom-loop / unexpected-envelope / max-rounds, call `emitAgentTurn(emit, new RunCompleted({ status: "failed", summary: "...", abort: { reason } }))` BEFORE the return. The existing `emitMessageChunk` + `Effect.logWarning` calls stay intact (informational; same human-readable shape the local-agent emits). Reasons: kebab-case identifiers — `doom-loop`, `unexpected-envelope`, `max-rounds`. Richer detail (toolName, round, threshold) goes into the human-readable `summary` string. |
| `packages/evals/tests/gemini-react-loop.test.ts` | edit | (1) Extended the existing doom-loop test in place to assert the terminal `agent_turn` is a `RunCompleted` instance with `status === "failed"` and `abort.deepStrictEqual({ reason: "doom-loop" })`. (2) Added a new test, `runGeminiReactLoop max-rounds termination`, that feeds 20 THOUGHT envelopes through the mock model and asserts the synthetic terminal envelope has `abort.reason === "max-rounds"`. (3) Documented why the unexpected-envelope branch isn't unit-tested: `parseAgentTurn` (Schema.decodeUnknownEffect on the closed `AgentTurn` union) rejects unknown tags upstream of the instanceof chain — the synthetic emit is defense-in-depth for a future variant added without updating the chain. |
| `packages/evals/evals/wave-r5-ab.eval.ts` | edit | Add `EVAL_TASK_FILTER` (sibling to `EVAL_R5_SKIP_RUNNERS`): comma-separated EvalTask ids; default empty (all tasks). Required for the partial-sweep verification below. Same `Config.string(...).pipe(Config.withDefault(""))` pattern as `EVAL_R5_SKIP_RUNNERS`. |

### Architectural decisions

1. **Extend the `RunCompleted` envelope schema, do NOT add a separate
   "synthetic" type.** The supervisor's reducer already pattern-matches on
   `RunCompleted` instances. Inventing a parallel `SyntheticRunCompleted`
   class would force the reducer (and the AgentTurn union, and parseAgentTurn,
   and every consumer) to handle two terminal envelopes — far more invasive
   than adding one optional field. The optional field is also semantically
   accurate: a model COULD legitimately emit `abort` on a non-synthetic
   RunCompleted (e.g. "I'm aborting because the page requires auth"), and
   the supervisor's gate logic already handles that case.

2. **Reasons are kebab-case identifiers, not free-form strings.** Three
   choices: `doom-loop`, `unexpected-envelope`, `max-rounds`. Stable
   identifiers make these greppable in logs and let downstream scoring
   (e.g. wave-r5-ab aggregator) bucket abort reasons without parsing.
   Richer per-occurrence detail (toolName, round count) lives in `summary`.

3. **No fix for `packages/local-agent/src/tool-loop.ts`.** Per task spec
   ("DO NOT touch local-agent"), and confirmed by source inspection: the
   production runner has the same three gaps but the 20 valid gemma-react
   traces in `evals/traces/wave-r5-ab/` all completed (4 with
   `run_finished:passed`, 16 with `stream_ended` — Gemma never tripped
   the early-exit paths during the sweep). Filed
   `~/.claude/projects/.../memory/project_local_agent_termination_audit.md`
   documenting the gap, why it's not in flight on Gemma, when to fix, and
   the (now identical) fix pattern.

4. **`EVAL_TASK_FILTER` config knob.** Pure additive change, mirrors the
   existing `EVAL_R5_SKIP_RUNNERS` knob. Required for the partial-sweep
   verification (one task, one runner, ~50s) without re-paying API cost on
   the other 19 tasks. Useful for any future surgical re-runs.

### Verification

#### Unit tests

```
$ pnpm --filter @neuve/evals exec vp test run tests/gemini-react-loop.test.ts --reporter=verbose
 ✓ runGeminiReactLoop happy path > emits agent_turn for THOUGHT, dispatches ACTION through mcpBridge, terminates on RUN_COMPLETED  8ms
 ✓ runGeminiReactLoop PLAN_UPDATE flow > dispatches PLAN_UPDATE envelopes through agent_turn so the supervisor's reducer sees them  1ms
 ✓ runGeminiReactLoop doom loop detection > aborts after N identical consecutive ACTION envelopes  2ms
 ✓ runGeminiReactLoop max-rounds termination > emits a synthetic RunCompleted with abort.reason='max-rounds' after MAX_TOOL_ROUNDS non-terminal envelopes  7ms
 ✓ runGeminiReactLoop error propagation > surfaces a GeminiReactCallError when generateObject throws  1ms
 ✓ runGeminiReactLoop schema-violation guard > re-validates AgentTurn output via Effect Schema and fails loud on a non-conforming envelope  1ms
 Test Files  1 passed (1)
      Tests  6 passed (6)
   Duration  273ms
```

#### Full evals + supervisor + shared suite

```
$ pnpm --filter @neuve/evals test
 Test Files  17 passed (17)
      Tests  173 passed (173)
   Duration  1.80s

$ pnpm --filter @neuve/supervisor test
 Test Files  14 passed (14)
      Tests  134 passed (134)
   Duration  1.58s

$ pnpm --filter @neuve/shared test
 Test Files  15 passed (15)
      Tests  231 passed (231)
   Duration  340ms
```

Test count went from 172 → 173 in `@neuve/evals`. The doom-loop test was
extended in place (no new test file); the `max-rounds` test is the +1.

#### Live smoke test (regression guard from T1, still green)

```
$ pnpm --filter @neuve/evals exec vp test run tests/gemini-live-smoke.test.ts --reporter=verbose
 ✓ tests/gemini-live-smoke.test.ts > gemini-react live smoke > emits a valid AgentTurn envelope from gemini-3-flash-preview 1720ms
 Test Files  1 passed (1)
      Tests  1 passed (1)
   Duration  1.94s
```

The schema extension on `RunCompleted` did not break Gemini's structured
output — the live smoke test still parses a valid envelope from
`gemini-3-flash-preview` in <2s. Optional fields stay optional in the
inlined JSON Schema sent to Google.

#### Live partial sweep — the headline evidence

Goal: prove a single calibration task that doom-looped in run-3 now exits
cleanly (within seconds, not the 600s testTimeout).

```
$ EVAL_R5_SKIP_RUNNERS=gemma-react,gemma-oracle-plan \
  EVAL_TASK_FILTER=calibration-3-two-step-docs \
  pnpm --filter @neuve/evals eval:wave-r5-ab
```

Captured in `/tmp/wave-r5-ab-r5b/r5b-t3-doom-loop-fix.log`:

- `[16:59:56.189]` Real runner starting — `taskId: 'calibration-3-two-step-docs'`.
- `[17:00:44.548]` `WARN: Gemini-react max rounds reached { maxRounds: 15 }`.
  Note: this run hit max-rounds, not doom-loop — the model ran out of
  rounds before repeating identically 3x. Either way the early-termination
  path is exercised end-to-end.
- `[17:00:44.562]` Real runner finished — `toolCalls: 9, turnCount: 15`.
- Total wall clock: **48,395 ms** (~48 seconds).
- Pre-fix wall clock for this exact failure mode: **600,000 ms** (the
  testTimeout, since no RunFinished ever reached the stream).
- **Wall-clock saved per early-termination event: ~552 seconds (~9.2 minutes).**

Trace evidence in `evals/traces/wave-r5-ab/gemini-react__calibration-3-two-step-docs.ndjson`:

```json
{"type":"status_marker","ts":1777215644550,"marker":"RUN_COMPLETED","payload":["failed","Reached maximum tool call rounds (15). Stopping."]}
{"type":"stream_terminated","ts":1777215644562,"reason":"run_finished:failed","remainingSteps":0}
```

The `stream_terminated` reason is `run_finished:failed` (not `stream_ended`,
not a timeout) — proving the supervisor's `Stream.takeUntil(executed.hasRunFinished)`
predicate fired on the synthetic envelope, the reducer forwarded
`envelope.abort` to RunFinished, and `runFinishedSatisfiesGate` returned
true via the `lastRunFinished.abort !== undefined` short-circuit.

### Verification cross-check: production runner audit

Source inspection of `packages/local-agent/src/tool-loop.ts` confirms the
same three gaps exist there (doom-loop @ ~317-342, max-rounds @ ~492-501,
unexpected envelope @ ~482-490). Per task spec ("DO NOT touch local-agent"),
no code change. Filed memory note
`project_local_agent_termination_audit.md` documenting the gap, why
it's not in flight (Gemma's 20 wave-r5 traces all completed without
tripping the early-exit paths), and the fix pattern when needed.

### Process

- All work on top of T1 final commit `26e40ecd`. Working tree at session
  start was clean except for pre-existing untracked Q9 probes +
  `.claude/scheduled_tasks.lock`. The 20 `gemma-react__*.ndjson` traces
  in `packages/evals/evals/traces/wave-r5-ab/` were untouched.
- The verification run did regenerate
  `gemini-react__calibration-3-two-step-docs.ndjson` (originally from
  run-3, now from the fixed-loop verification). This is intentional —
  the new trace IS the evidence the fix works. The other 19 wave-r5
  task traces stayed untouched.
- No `git stash` / `git reset` / `git checkout --` / `--no-verify` /
  `--amend` / `git push` per `feedback_reviewer_never_stash.md`.

### Outstanding / for reviewer

Reviewer (T4) should challenge:

- **Schema extension vs. summary-string-only encoding.** I chose to extend
  the `RunCompleted` envelope with an optional `abort: { reason }` field
  rather than packing the abort metadata into `summary`. The reason: the
  supervisor's `runFinishedSatisfiesGate` keys on `lastRunFinished.abort
  !== undefined` to short-circuit. With summary-only encoding, the gate
  would still strip the RunFinished if `plan.allPlanStepsTerminal` is
  false (which it is when an early-exit fires). Verify this reasoning
  matches the executor logic at `executor.ts:191-211`.
- **Reducer one-line edit.** `handleRunCompleted` now forwards
  `envelope.abort` to `RunFinished.abort`. Existing reducer tests cover
  the natural-path `RunCompleted({ status, summary })` shape — `abort`
  defaults to undefined, so no test breakage. New behavior (synthetic
  terminal envelopes) is exercised by the new unit test on the
  loop side.
- **Why the live verification hit max-rounds, not doom-loop.** Gemini
  doesn't always doom-loop deterministically — sometimes it makes 15
  unique-but-failing tool calls and hits max-rounds. Both paths are
  now fixed; both unit tests exist (one extended in place, one added).
  The live evidence covers the max-rounds path; the unit test covers
  doom-loop.
- **The unexpected-envelope branch is defense-in-depth.** No unit test
  for it because `parseAgentTurn` rejects unknown tags upstream. If
  AgentTurn ever grows a new variant without updating the
  `runGeminiReactLoop` instanceof chain, this branch fires — and now
  emits a clean terminal envelope instead of hanging. Acceptable as
  unreachable-today defense per task spec.
