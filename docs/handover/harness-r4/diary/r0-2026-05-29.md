# Phase Diary — harness-r4-detectors

## What we did

**Date:** 2026-05-29
**HEAD:** `064ce7c7` on `gemma-harness-lora`

**Objective:** Implement the two fully-spec'd harness-r4 detector candidates from the r3 handover:

1. **Vary-each-attempt rejection detector** ✅ — DONE
2. **THOUGHT-only loop detector** ✅ — DONE

**Verdict:** NEEDS REVIEW — Ready for strict-critique audit

### Implementation Summary

**Vary-each-attempt detector:**
- Modified `trackRejection` to maintain separate streaks per `stepId`
- Shape variations no longer reset streaks (closes P3 autopsy finding)
- Updated both `tool-loop.ts` and `gemini-react-loop.ts`
- Logs: `reflect injection (parse-fail)` and `reflect injection (tool-error)` now include `streakLength`

**THOUGHT-only loop detector:**
- Added `THOUGHT_ONLY_THRESHOLD=5` and `THOUGHT_LOOP_ABORT_THRESHOLD=6`
- Tracks consecutive THOUGHTs per step without ACTION/progress
- REFLECT fires at threshold (5), abort at threshold+1 (6)
- Updated both `tool-loop.ts` and `gemini-react-loop.ts`
- Logs: `reflect injection (thought-only)` and `THOUGHT-only loop detected`

### Files Modified

**Local Agent** (`packages/local-agent/src/tool-loop.ts`):
- Line ~36-39: Added `THOUGHT_ONLY_THRESHOLD=5`, `THOUGHT_LOOP_ABORT_THRESHOLD=6`
- Line ~153-156: Added `thoughtOnlyStreak` and `reflectInjectedThoughtLoop` tracking
- Line ~162-205: Rewrote `trackRejection` for vary-each-attempt (Map-based streaks)
- Line ~196-199: Updated `resetRejectionStreak` to accept `stepId` param
- Line ~387-438: Added THOUGHT-only loop detection in Thought handler
- Line ~444-447: Clear THOUGHT streak on `PlanUpdate`
- Line ~470-473: Clear THOUGHT streak on `StepDone`

**Evals Runner** (`packages/evals/src/runners/gemini-react-loop.ts`):
- Line ~19-21: Imported new thresholds
- Line ~318-320: Added `thoughtOnlyStreak` and `reflectInjectedThoughtLoop` tracking
- Line ~324-375: Rewrote `trackRejection` for vary-each-attempt (Map-based streaks)
- Line ~354-357: Updated `resetRejectionStreak` to accept `stepId` param
- Line ~437-492: Added THOUGHT-only loop detection in Thought handler
- Line ~498-501: Clear THOUGHT streak on `PlanUpdate`
- Line ~507-510: Clear THOUGHT streak on `StepDone`

**Constants** (`packages/evals/src/runners/gemini-react-constants.ts`):
- Line ~24-25: Added `GEMINI_REACT_VARY_EACH_ATTEMPT_THRESHOLD=2`
- Line ~30-32: Added `GEMINI_REACT_THOUGHT_ONLY_THRESHOLD=5`
- Line ~33: Added `GEMINI_REACT_THOUGHT_LOOP_ABORT_THRESHOLD=6`

### Build & Typecheck

- ✅ `pnpm --filter @neuve/local-agent build` — SUCCESS
- ✅ `pnpm --filter @neuve/evals typecheck` — SUCCESS
- ⏳ `pnpm test` — Pending
- ⏳ `pnpm check` — Pending

## Next Steps

1. [x] Implement vary-each-attempt rejection detector
2. [x] Implement THOUGHT-only loop detector
3. [ ] Add fixture tests for both detectors
4. [ ] Run `pnpm test` to verify
5. [ ] Run `pnpm check` (typecheck + lint + format)
6. [ ] Request reviewer audit via /skill:strict-critique

---

**Status:** IMPLEMENTATION COMPLETE
**Next Review:** Request reviewer audit
