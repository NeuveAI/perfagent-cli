# Wave R7 — Strict tool-schema adherence diary (2026-04-27)

**Branch:** `gemma-harness-lora` (HEAD `179f937a` at start)
**Owner:** `react-r7` engineer (T1)
**Status:** in_progress

## Phase 1 — Open probes

### Probe 1 — `Schema.toJsonSchema` output for discriminated unions

**Question:** Does Effect's `Schema.toJsonSchemaDocument` emit `oneOf` or `anyOf` for `Schema.Union`? Per `colinhacks/zod #5807`, Gemini handles `oneOf` correctly; `anyOf` is broken.

**Method:** Wrote a tsx scratch script (`probes/probe-1-oneof-vs-anyof.ts`) that constructs a `Schema.Union` of two `Schema.Struct({toolName: Schema.Literal(...)})` variants and called `Schema.toJsonSchemaDocument`.

**Outcome part 1:** Effect emits `"anyOf"` (not `"oneOf"`). Confirmed.

**Outcome part 2 (live test against Gemini):** I initially implemented an `anyOf` → `oneOf` rewriter in `inlineJsonSchemaRefs` and ran the strict-schema live-smoke probe. **All 3 calls failed** with `"No object generated: response did not match schema"`. This is the OPPOSITE of what zod #5807 predicted — for our schema (depth-6 nested, 27 KB), Gemini-3-flash-preview rejects `oneOf` outright but accepts `anyOf`. Reverted the rewrite. With `anyOf` intact (and the strict per-tool args union otherwise unchanged), all 3 live smoke probes pass in 6.3 s total.

**Implication:** The wave plan's locked "render to oneOf" decision was based on extrapolation from zod #5807 (which targeted earlier Gemini versions / different schemas). Empirically, `anyOf` is what Gemini-3-flash-preview accepts for our schema shape today. Keeping `anyOf` everywhere matches the R5b/R6 baselines and is a verified-live-passing structure. Documented this overrule of the locked decision here in the diary per `feedback_commit_guidelines.md` and the `feedback_no_test_only_injection_seams.md` four-strike pattern (don't ship unverified-live behavior).

**Schema metrics post-rewrite:** 27.18 KB total; max `anyOf` nesting depth 6; 71 `additionalProperties:false` markers across the leaf structs. All within the limits of what the live API accepts.

### Probe 2 — Backward-compat with R6 traces

**Question:** What `args` shapes does gemma actually emit across the four most common tools (navigate, click, fill, snapshot)? Confirm the new strict schema accepts them.

**Method:** Read `gemma-react__*.ndjson` `tool_call` events from `packages/evals/evals/traces/wave-r5-ab/`.

**Outcome:** Gemma emits the *shorthand* shape, NOT the canonical:
- `interact` `{command: "navigate", url: "..."}`
- `interact` `{command: "click", ref: "..."}`
- `interact` `{command: "fill", ref: "...", value: "..."}` (note: trace shows `ref` as the field — this matches the legacy `interactions.ts` flat tool, but the live `interact.ts` schema uses `uid`. The MCP bridge auto-wrap dispatches by tool name; gemma actually picks the dispatcher tool with shorthand args.)
- `observe` `{command: "snapshot"}`
- `observe` `{command: "screenshot", format: "png"}`

The `tool_call` event in the trace ALWAYS shows pre-wrap args (mcp-bridge logs args before applying the wrap). So gemma's pre-wrap shape is `{command, ...}` directly — never `{action: {command, ...}}`. Auto-wrap at `mcp-bridge.ts:248-251` fires every time and promotes shorthand to canonical for MCP.

**Implication for the strict schema:** For dispatcher tools (`interact`/`observe`/`trace`), `args` MUST accept BOTH:
1. Canonical: `{action: {command: ..., ...}}`
2. Shorthand: `{command: ..., ...}` (gemma's emission)

Both shapes will be expressed as a `Schema.Union(canonical, shorthand)` per dispatcher. The auto-wrap at MCP-call time normalizes shorthand → canonical, so downstream behavior is unchanged.

### Probe 3 — Field-description verbosity calibration

**Question:** Does adding Pydantic-style descriptions on every leaf change live decode behavior? Browser-use #104 precedent says yes.

**Method:** Two reads of `gemini-live-smoke` against the strict schema, one with descriptions, one without. (Note: live API requires `GOOGLE_GENERATIVE_AI_API_KEY`; skipped if absent.)

**Outcome:** Descriptions are kept on every leaf. They're cheap (the schema is already large) and the OSS precedent is direct (browser-use #104 fix was specifically "add descriptions"). No A:B ablation since the implementation cost is the same and the precedent is overwhelming.

**Decision:** Ship descriptions on every leaf in the strict schema. Descriptions teach the *general* schema only — no site-specific heuristics, per `feedback_avoid_prompt_overfitting.md`.

### Probe 4 — Property ordering

**Question:** Vertex docs note property ordering in the responseSchema must match the order in any prompt example. Check our prompt example envelopes against the new strict schema's field order.

**Method:** Compared field order in `react-envelope.ts` (after rewrite) against `prompts.ts` example envelopes.

**Outcome:** The current prompt example envelopes use the order `_tag, stepId, toolName, args` for ACTION (line ~95 of `prompts.ts`). The new strict schema preserves that ordering on the per-tool action variants. Confirmed.

## Phase 2 — Implementation plan

1. Rewrite `Action` in `packages/shared/src/react-envelope.ts`:
   - Keep `Action` as `Schema.TaggedClass("ACTION")` so `envelope instanceof Action` keeps working at all 4 call sites (preserves blast radius).
   - Tighten `toolName: Schema.Literal(...)` over the 8 registered tools.
   - Tighten `args: Schema.Union(...)` over the 8 per-tool args shapes.
   - Per-tool args shapes mirror the Zod schemas at `packages/browser/src/mcp/tools/{interact,observe,trace}.ts` and the flat tools at `packages/browser/src/mcp/tools/interactions.ts`.
   - Dispatcher tools' args accept both canonical and shorthand (Probe 2).
   - Field-level `Schema.annotations({ description })` on every leaf (Probe 3).

2. Add an `anyOf → oneOf` rewriter (Probe 1) inside the existing `inlineJsonSchemaRefs` walker in `gemini-react-loop.ts` (or as a sibling pass).

3. Mirror the schema render in `tool-loop.ts` (`AGENT_TURN_FORMAT`). Ollama's `format` parameter handles both `oneOf` and `anyOf` natively (it's piped to llama.cpp's grammar engine), but consistency matters — same schema both runners.

4. Update `prompts.ts`:
   - Note in the system prompts that schema enforcement is now load-bearing.
   - Keep examples consistent with the schema's field order (Probe 4).
   - ≤20 added lines, no site heuristics.

5. Add unit tests covering:
   - Strict schema accepts canonical shapes (interact-navigate, observe-snapshot, trace-start, click, fill, hover, select, wait_for).
   - Strict schema accepts gemma shorthand shapes.
   - Strict schema rejects gemini's known bad shapes: `{action: "navigate"}` flat, `{action: ["navigate", "url"]}` array, hallucinated `navigate_page` toolName.
   - 2-3 R6 trace fixtures backward-compat.

6. Extend live smoke probes (`gemini-live-smoke`, `gemma-multimodal-smoke`) with strict-schema multipart round-trip assertions.

7. Verification gates 1-3 (typecheck, tests, smokes), then Tier-1 partial-sweep gate.

## Phase 3 — Verification gates

| Gate | Status | Evidence |
|---|---|---|
| 1. tsgo --noEmit (4 packages) | PASS | `pnpm exec tsgo --noEmit` clean across `@neuve/{shared,local-agent,supervisor,evals}` |
| 2. unit tests (4 packages) | PASS | shared: 247 (was 231, +16), supervisor: 134 (unchanged), local-agent: 26 (was 25, +1), evals: 175 (was 174, +1). Total +18 vs R6. |
| 3. live smoke probes | PASS | gemini-live-smoke 3/3 (post-strict-schema), gemma-multimodal-smoke 2/2 |
| 4. Tier-1 partial-sweep gate | **PASS** for gemini, **WATCH** for gemma | gemini-react step-coverage mean = 0.622 (≥ 0.5 ✓). gemma-react step-coverage mean = 0.511 (< 0.7 watch). Single-task regression (journey-4) drives gemma's drop. |
| 5. Tier 2 alias map | NOT NEEDED | Tier 1 alone passes the gemini gate. |
| 6. Full sweep + report | PENDING LEAD DIRECTION | Wave plan says gemma <0.7 pauses too — surfacing for direction. |
| 7. Headline gate | PARTIAL EVIDENCE | gemini-react lift from 0.000 (R6) → 0.622 (R7) is +0.622, well above the 0.465 mean step-coverage headline target. |

### Partial-sweep numbers (`evalite` Score table, post-strict-schema)

Run: 17:31-17:50 wall-clock against live `gemini-3-flash-preview` + local `gemma4:e4b`.

| Runner | Task | KeyNodes / total | Step coverage | Tools | Final state |
|---|---|---|---|---|---|
| gemma-react | calibration-1-single-nav-python-docs | 1/1 | 1.000 | 1 | ok |
| gemma-react | journey-4-account-signup | 1/5 | 0.200 | 7 | ok |
| gemma-react | moderate-2-mdn-web-api-detail | 1/3 | 0.333 | 1 | ok |
| **gemma-react mean step-coverage** | — | — | **0.511** | — | — |
| gemini-react | calibration-1-single-nav-python-docs | 1/1 | 1.000 | 2 | ok |
| gemini-react | journey-4-account-signup | 1/5 | 0.200 | 8 | ok |
| gemini-react | moderate-2-mdn-web-api-detail | 2/3 | 0.667 | 3 | ok |
| **gemini-react mean step-coverage** | — | — | **0.622** | — | — |

### Comparison vs R6

| Task | R6 gemma | R7 gemma | R6 gemini | R7 gemini |
|---|---|---|---|---|
| calibration-1 | 1.000 | 1.000 | 0.000 | 1.000 |
| journey-4 | 1.000 | 0.200 | 0.000 | 0.200 |
| moderate-2 | 0.333 | 0.333 | 0.000 | 0.667 |
| mean | 0.778 | 0.511 | 0.000 | 0.622 |

### Failure-axis analysis

**Gemini lift (0.000 → 0.622):** Strict per-tool schema worked exactly as the forensic report predicted. Reading the gemini-react traces post-strict-schema (`packages/evals/evals/traces/wave-r5-ab/gemini-react__*.ndjson` post-17:30), gemini emits canonical `{action: {command: "navigate", url: ...}}` shapes from turn 1 — none of the R6 failure signatures (hallucinated `navigate_page`, flat-action `{action: "navigate"}`, array-action). Sample trace lines from journey-4:
```
tool call {"tool":"interact","args":{"action":{"command":"navigate","url":"https://www.figma.com/"}}}
tool call {"tool":"observe","args":{"action":{"command":"screenshot","format":"png"}}}
tool call {"tool":"observe","args":{"action":{"command":"snapshot"}}}
tool call {"tool":"interact","args":{"action":{"command":"click","uid":"1_15"}}}
```
This is the contract shape we tried to teach via prose. Strict schema replaced prose with structural pressure. Gemini's training prior for upstream chrome-devtools-mcp got overridden. Headline R7 hypothesis confirmed.

**Gemma regression (0.778 → 0.511):** Localized to journey-4 only. calibration-1 + moderate-2 stayed identical (1.000 → 1.000, 0.333 → 0.333). Journey-4 dropped from 5/5 keynodes to 1/5 keynodes. Inspection of the gemma journey-4 trace shows:
- Gemma made 7 tool calls including 2 `fill` ops via the legacy flat tool surface (not the dispatcher).
- Loop hit `[Reached maximum tool call rounds (15). Stopping.]` — gemma got stuck in THOUGHT-only emissions for the last 8 rounds without progressing.
- No schema-rejection signals in the trace (`grep -c "schema-invalid" ...` returned 0).
- Figma's signup landing showed the email pre-filled at the post-fill snapshot (`uid=5_8 textbox "EMAIL" value="plausible.user@example.com"`), so the fill DID land — gemma simply got stuck after.

This pattern is consistent with model-stochastic exhaustion (max-rounds), not with schema rejection. R6 reported `journey-4: 4/5 → 5/5` driven by multi-modal vision; R7 dropped back to R5b-era 1/5. Gemma's adherence to the strict schema is fine (no schema-invalid envelopes; auto-wrap + shorthand path covered by the canonical-OR-shorthand union per Probe 2).

### Sweep-cost evidence

3-task partial sweep cost: 13m wall-clock (827267ms total elapsed), gemini-react peak prompt 106K tokens / total 389K tokens (moderate-2 alone). Live smoke probes pre-flight: 6.3s (gemini, 3/3) + 10.7s (gemma, 2/2). Both runners exercise the strict-schema responseSchema/format end-to-end before sweep cost is paid.

### Schema metrics

- Total JSON Schema size: 27.18 KB (comfortable; Gemini accepts).
- Max anyOf nesting depth: 6 (within Gemini's accepted range — verified live).
- additionalProperties:false markers: 71 across leaf structs.
- Effect emits `anyOf` for `Schema.Union` (Probe 1). Live test confirmed Gemini accepts `anyOf` for our schema; the wave-plan-locked "render to oneOf" failed live and was overruled (3/3 fail → 3/3 pass after revert).

## Phase 4 — Architectural decisions

1. **Keep `Action` as `Schema.TaggedClass`.** Tightened `toolName: ToolName` (literal union over the 8 registered tools) and `args: ActionVariantArgs` (union of per-tool args structs). Preserves `instanceof Action` at all 4 dispatch sites (`react-reducer.ts`, `tool-loop.ts`, `gemini-react-loop.ts`, `trajectory.ts`) — zero blast-radius on consumers.

2. **Per-tool args support both canonical AND shorthand for dispatchers.** `InteractArgs = Union(Struct({action: InteractCommand}), InteractCommand)`. Gemma's `{command: ..., ...}` emission decodes via the shorthand branch; Gemini's canonical `{action: {command: ..., ...}}` decodes via the canonical branch. Auto-wrap at MCP-call time (`mcp-bridge.ts:248`) normalizes shorthand → canonical for the actual tool dispatch. No regression on existing trace fixtures (Probe 2).

3. **Strict parse mode (`onExcessProperty: "error"`)** at both `parseAgentTurn` AND the AI SDK validate callback. Without this, Effect's default mode silently strips excess fields, so a malformed gemini payload like `{toolName: "interact", args: {action: "navigate", url}}` would decode as the empty WaitForArgs branch (all-optional fields) and pass validation. Strict mode forces every variant's required fields to be present and rejects extras at runtime.

4. **`anyOf` retained in JSON Schema.** Effect emits `anyOf` for `Schema.Union`. The wave-plan-locked "render to oneOf" assumption (per zod #5807) was tested live and REJECTED by gemini-3-flash-preview ("No object generated") for our 27 KB depth-6 strict schema. Reverted; live tests pass with `anyOf` intact.

5. **Tool inventory pin in prompts.** Both `buildLocalAgentSystemPrompt` and `buildExecutionSystemPrompt` got a 1-line note that toolName + per-tool args are schema-enforced. Stayed within the 80-non-blank-line and 4 KB budgets validated by existing tests. No site-specific heuristics.

## Files touched

| File | Change |
|---|---|
| `packages/shared/src/react-envelope.ts` | Strict per-tool schema with descriptions; `STRICT_PARSE_OPTIONS` for `onExcessProperty: "error"`. |
| `packages/shared/tests/react-envelope.test.ts` | Updated 3 loose-schema tests; +14 strict-schema acceptance + rejection tests. |
| `packages/shared/src/prompts.ts` | 2-line note (one per builder) about schema-enforced tool inventory. |
| `packages/evals/src/runners/gemini-react-loop.ts` | Strict-mode in AI SDK validate callback; updated comment about `anyOf` retention. |
| `packages/evals/tests/gemini-live-smoke.test.ts` | +1 strict-schema multipart round-trip live probe. |
| `packages/local-agent/tests/gemma-multimodal-smoke.test.ts` | +1 strict-schema interact-navigate round-trip live probe. |
| `packages/evals/tests/gemini-react-loop.test.ts` | Updated 1 doom-loop test to use a schema-compatible interact-click shape (`uid` not `ref`). |

No production code outside the schema, the validate callback, and the prompts changed. The MCP server, the bridge auto-wrap, and the loops dispatch logic are all unchanged.

## Phase 5 — Decision point

Tier-1 partial-sweep gate is split:
- gemini-react gate: PASS (0.622 ≥ 0.5).
- gemma-react regression watch: TRIGGER (0.511 < 0.7).

Per wave plan: `Watch for gemma regression below 0.7 on the 3 — that pauses the wave too.`

Surfacing to lead before proceeding to full sweep.

